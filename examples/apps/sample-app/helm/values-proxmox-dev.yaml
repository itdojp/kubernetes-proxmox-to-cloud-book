replicaCount: 1

image:
  repository: docker.io/hashicorp/http-echo
  tag: "latest"

app:
  text: "sample-app (helm proxmox-dev)"

gateway:
  enabled: true
  className: eg
  host: sample-app.local

ingress:
  enabled: false
