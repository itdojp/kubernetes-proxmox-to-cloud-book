{
  "name": "kubernetes-proxmox-to-cloud-book",
  "version": "0.1.0",
  "private": true,
  "description": "Proxmox上の検証Kubernetesからクラウド上の本番Kubernetesへ移行するための設計・手順・運用を、現実的な差分（LB/Storage/Identity/Observability等）を前提に整理する実務ガイド。",
  "license": "CC-BY-NC-SA-4.0",
  "author": "ITDO Inc.",
  "repository": {
    "type": "git",
    "url": "https://github.com/itdojp/kubernetes-proxmox-to-cloud-book.git"
  },
  "scripts": {
    "check:metadata": "node scripts/check-metadata-consistency.js",
    "check:proxmox-version-snapshot": "node scripts/check-proxmox-version-snapshot.js",
    "check:proxmox-version-snapshot:self-test": "node scripts/check-proxmox-version-snapshot.js --self-test",
    "check:ingress-controller-policy": "node scripts/check-ingress-controller-policy.js",
    "check:ingress-controller-policy:self-test": "node scripts/check-ingress-controller-policy.js --self-test",
    "check:introduction-appendix-links": "node scripts/check-introduction-appendix-links.js",
    "check:introduction-appendix-links:self-test": "node scripts/check-introduction-appendix-links.js --self-test",
    "check:introduction-appendix-links:built": "node scripts/check-introduction-appendix-links.js --built-site _site",
    "check:security": "npm audit",
    "test": "npm run check:metadata && npm run check:proxmox-version-snapshot && npm run check:proxmox-version-snapshot:self-test && npm run check:ingress-controller-policy && npm run check:ingress-controller-policy:self-test && npm run check:introduction-appendix-links && npm run check:introduction-appendix-links:self-test",
    "dev": "cd docs && (bundle check || bundle install) && bundle exec jekyll serve --livereload --baseurl ''",
    "build": "cd docs && (bundle check || bundle install) && bundle exec jekyll build",
    "dev:podman": "bash scripts/jekyll-podman.sh serve",
    "build:podman": "bash scripts/jekyll-podman.sh build"
  },
  "homepage": "https://itdojp.github.io/kubernetes-proxmox-to-cloud-book/",
  "bugs": {
    "url": "https://github.com/itdojp/kubernetes-proxmox-to-cloud-book/issues"
  }
}
